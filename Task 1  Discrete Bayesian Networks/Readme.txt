All the dataset and config files are uploaded in the below path
'*\Dataset and config files'

1. Run the 'BayesNetExactInference.py' changing the paths of respective config files
2. Run the *.ipynb files by changing the paths of respective *train*.csv files