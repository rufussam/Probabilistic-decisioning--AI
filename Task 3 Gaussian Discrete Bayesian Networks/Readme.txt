All the dataset and config files are uploaded in the below path
'*\Dataset and config files'

the continuous data vs discrete dataset, PDF (Probability Density Function) are in the current path.

1. Run the 'BayesNetExactInference_GBN with compute and scoring.py' changing the paths of CPT (Conditional Probability Table), train and test files
